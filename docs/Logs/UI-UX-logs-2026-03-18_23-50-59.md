# UI/UX Log Sheet

Timestamp: 2026-03-18 23:50:59

## Summary

Fixed the newer runtime failure `__webpack_modules__[moduleId] is not a function` and the related dev-server chunk instability that appeared after introducing `src/app/head.tsx` for Material Symbols loading.

## Root Cause

- The dedicated `src/app/head.tsx` file triggered unstable dev bundling behavior in this app setup
- The runtime errors included React client manifest lookup failures and recurring stale chunk errors in `.next`

## Fix Applied

- Removed `src/app/head.tsx`
- Moved the global Material Symbols stylesheet link into `src/app/layout.tsx` inside the document head
- Cleared `.next`
- Restarted `next dev`

## Verification

- Confirmed `200` responses for:
  - `/`
  - `/settings`
  - `/login`
  - `/signup`
  - `/projects`
- `npm run build` passed
- `npm run lint` completed with one existing warning about custom font loading in `src/app/layout.tsx`

## Note

- The current icon-font setup is stable again in dev, but Next still warns about the stylesheet link placement in `src/app/layout.tsx`
